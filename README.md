# Notas:
Este es mi primer programa en Node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base de la tabla de multiplicar       [number] [required]
  -l, --listar   Genera la lista por consola de la tabla con sus 10 valores
                 correspondientes                     [boolean] [default: false]
  -h, --hasta    Es el limite que va a tener la tabla de multiplicar
                                                          [number] [default: 10]

```